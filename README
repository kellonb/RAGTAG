# to install RAGTAG genA
make install 

# to clean up object files
make clean 

# for debugging purpose
make debug

# run test cases 
make test


RAGTAG Manual can be found here, read this first. 
https://docs.google.com/document/d/1VHVWCPeBlzBsUrCb-7jB6FpM4wwmpJuyWXdh-nNtmec/edit 

To run RAGTAG but look at examples in the manual and in the test case directory
./genA -i genA_input -p parmfile.4 -r CNX_4p.restart -s CNX_4p.scores -f CNX_4p.frcmod -y CNX_4p.fit -o CNX_4p.log
# to restart your previous run
./genA -i genA_input -p parmfile.4.1 -c CNX_4p.restart -r CNX_4p.restart1 -s CNX_4p.scores1 -f CNX_4p.frcmod1 -y CNX_4p.fit1 -o CNX_4p.log1

# the scripts directory have some usefule scripts, Details on those coming soon


